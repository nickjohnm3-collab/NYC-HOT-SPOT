# Google Maps Places API key (required for real restaurant photos on cards)
# 1. Enable the "Places API (New)" in Google Cloud Console.
# 2. Restrict the key to HTTP referrers (your domain) to prevent unauthorised use.
# Copy this file to .env and fill in your key:
REACT_APP_GOOGLE_MAPS_API_KEY=your_api_key_here
